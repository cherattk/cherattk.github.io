const AppEvent = require('./eventstore').AppEvent;

function DataManager() {

  this.setProject = function(project_data) {
    
  }

  this.getList = function (storeName , criteria) {
    var store = "";
    return store;
  }



}

module.exports = new DataManager();